[phases.setup]
aptPkgs = ["python3", "gcc"]

[phases.install]
cmds = ["python -m venv --copies /opt/venv", ". /opt/venv/bin/activate && pip install -r requirements.txt"]

[start]
cmd = "python manage.py migrate --noinput && gunicorn housekeeper_connect.wsgi:application --bind 0.0.0.0:$PORT"